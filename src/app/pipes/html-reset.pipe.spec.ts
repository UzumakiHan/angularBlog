import { HtmlResetPipe } from './html-reset.pipe';

describe('HtmlResetPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlResetPipe();
    expect(pipe).toBeTruthy();
  });
});
